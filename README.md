# disk-usage-mu

A WordPress must-use plugin that displays a website's disk usage in a metabox on the home screen. Requires macOS or Linux and access to the `du` command.

## installation

Copy `disk-usage.php` to `/wp-content/mu-plugins`.
